# CL-bench RL Framework Dependencies
# Core
torch>=2.1.0
# Qwen3 models need transformers>=4.51 (qwen3 architecture)
transformers>=4.51.0
accelerate>=0.28.0
datasets>=2.14.0
tqdm>=4.65.0
pyyaml>=6.0

# RL / policy gradient
trl>=0.7.0
peft>=0.7.0

# Multi-GPU training (8×H100)
deepspeed>=0.14.0

# Reward computation
nltk>=3.8.0
scikit-learn>=1.3.0
numpy>=1.24.0

# Visualisation
matplotlib>=3.7.0

# Judge LLM (optional, set use_llm_judge=True)
openai>=1.0.0
